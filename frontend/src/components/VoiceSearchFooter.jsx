export function VoiceSearchFooter({ language }) {
  return (
    <div className="mt-auto py-6 text-center text-sm text-gray-400">
      <p>Voice Search - Speak in English, translate to {language.value}</p>
    </div>
  );
}
